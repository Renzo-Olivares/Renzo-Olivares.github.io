import 'package:flutter_web/material.dart';

class ResumeRoute extends StatelessWidget {
  ResumeRoute({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Resume"),
    );
  }
}
